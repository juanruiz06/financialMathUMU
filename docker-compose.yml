services:
  api:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "3000:3000"
    environment:
      PORT: "3000"
      PYTHON_EXECUTABLE: "python3"
    restart: unless-stopped
